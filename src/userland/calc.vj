# VajraLang -- Vajra's own tiny language, v0.
# Compiled by tools/vajrac.c into a real Vajra-loadable program
# (build/calc.bin), seeded into storage at boot as "calc.bin",
# and launched from the shell with: run calc.bin

let a = 2 + 3 * 4;
let b = (2 + 3) * 4;
print a;
print b;
print a - b;
print 100 / 7;
print -(6 * 7);
